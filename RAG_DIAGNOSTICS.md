# 🔍 План диагностики: Чанки не подходят под запрос

## 📋 Чек-лист проверки

### 1. ✅ Проверка качества индекса

#### 1.1. Проверить, что индекс создан и не пустой
```bash
# Проверить наличие файла индекса
ls -lh index/index.json

# Проверить размер файла (должен быть > 0)
du -h index/index.json

# В Telegram боте
/indexinfo
```

**Что проверить:**
- ✅ Файл `index/index.json` существует
- ✅ Размер файла > 0 байт
- ✅ Количество чанков > 0
- ✅ Количество документов > 0

#### 1.2. Проверить содержимое индекса
```bash
# Посмотреть структуру индекса
cat index/index.json | jq '.totalChunks'
cat index/index.json | jq '.chunks[0] | {filePath, content: .content[0:100]}'
```

**Что проверить:**
- ✅ Чанки содержат осмысленный текст (не пустые, не только форматирование)
- ✅ Чанки разбиты правильно (не слишком маленькие, не слишком большие)
- ✅ Метаданные корректны (filePath, chunkIndex)

#### 1.3. Проверить эмбеддинги в индексе
```bash
# Проверить размерность эмбеддингов
cat index/index.json | jq '.chunks[0].embedding | length'

# Проверить, что эмбеддинги не нулевые
cat index/index.json | jq '.chunks[0].embedding[0:5]'
```

**Что проверить:**
- ✅ Размерность эмбеддингов одинакова для всех чанков
- ✅ Эмбеддинги не нулевые (не все значения = 0)
- ✅ Эмбеддинги не NaN и не Infinity

---

### 2. 🔎 Проверка поиска (косинусное сходство)

#### 2.1. Проверить логи поиска
Смотреть в логах приложения:
```
INFO  - Поиск релевантных чанков для запроса: <ваш запрос>
INFO  - Загружено X чанков из индекса
INFO  - Эмбеддинг запроса сгенерирован успешно, размерность: X
INFO  - Найдено X релевантных чанков (топ-5)
DEBUG - Чанк 1: <путь> (сходство: X.XXX)
```

**Что проверить:**
- ✅ Эмбеддинг запроса генерируется успешно
- ✅ Размерность эмбеддинга запроса совпадает с размерностью чанков
- ✅ Значения сходства (scores) не все близки к 0
- ✅ Топ-5 чанков имеют разумные значения сходства (> 0.3 для хорошего совпадения)

#### 2.2. Проверить значения сходства
**Нормальные значения косинусного сходства:**
- `0.8 - 1.0` - очень высокая релевантность
- `0.6 - 0.8` - высокая релевантность
- `0.4 - 0.6` - средняя релевантность
- `0.2 - 0.4` - низкая релевантность
- `< 0.2` - очень низкая релевантность (скорее всего не подходит)

**Если все значения < 0.3:**
- Проблема с качеством эмбеддингов
- Проблема с формулировкой запроса
- Проблема с моделью эмбеддингов

---

### 3. 🤖 Проверка эмбеддингов

#### 3.1. Проверить модель эмбеддингов
```bash
# Проверить переменную окружения
echo $OLLAMA_EMBEDDING_MODEL

# Проверить, что модель загружена в Ollama
curl http://127.0.0.1:11434/api/tags
```

**Что проверить:**
- ✅ Модель эмбеддингов установлена (по умолчанию: `nomic-embed-text`)
- ✅ Модель загружена в Ollama
- ✅ Используется одна и та же модель для индексации и поиска

#### 3.2. Проверить генерацию эмбеддинга для запроса
Смотреть в логах:
```
INFO  - Генерация эмбеддинга для запроса (длина: X символов, модель: nomic-embed-text)
INFO  - Эмбеддинг запроса сгенерирован успешно, размерность: X
```

**Что проверить:**
- ✅ Эмбеддинг генерируется без ошибок
- ✅ Размерность эмбеддинга запроса = размерности эмбеддингов чанков
- ✅ Эмбеддинг не пустой и не нулевой

#### 3.3. Тест: сравнить эмбеддинги похожих запросов
**Идея:** Если два похожих запроса дают очень разные эмбеддинги - проблема с моделью.

**Пример:**
- Запрос 1: "Что я планировал на четвертый квартал?"
- Запрос 2: "Какие планы на Q4?"

Эти запросы должны иметь похожие эмбеддинги (высокое косинусное сходство).

---

### 4. 📝 Проверка формата запроса

#### 4.1. Проверить формулировку запроса
**Плохие запросы:**
- ❌ "Что там?" (слишком общий)
- ❌ "Информация" (слишком короткий)
- ❌ "Найди все про..." (слишком общий)

**Хорошие запросы:**
- ✅ "Что я планировал на четвертый квартал 2025 года?"
- ✅ "Какие проекты связаны с AI Advent?"
- ✅ "Что написано в заметках про отношения с Катей?"

#### 4.2. Проверить язык запроса
**Важно:** Если документы на русском, а запрос на английском (или наоборот), сходство будет низким.

**Решение:**
- Использовать тот же язык, что и в документах
- Или использовать мультиязычную модель эмбеддингов

---

### 5. 🔧 Проверка параметров индексации

#### 5.1. Размер чанков (chunkSize)
**Текущее значение:** 1000 символов

**Проблемы:**
- Слишком маленькие чанки (< 200 символов) - теряется контекст
- Слишком большие чанки (> 2000 символов) - много шума, низкая точность

**Рекомендации:**
- Для вопросов о фактах: 500-1000 символов
- Для вопросов о концепциях: 1000-2000 символов

#### 5.2. Перекрытие чанков (chunkOverlap)
**Текущее значение:** 200 символов

**Проблемы:**
- Слишком маленькое перекрытие - теряется контекст на границах
- Слишком большое перекрытие - дублирование информации

**Рекомендации:**
- 10-20% от размера чанка (для 1000 символов = 100-200)

---

### 6. 🎯 Проверка качества найденных чанков

#### 6.1. Вручную проверить топ-5 чанков
После выполнения `/rag <запрос>`, проверить в логах:
```
DEBUG - Чанк 1: <путь> (сходство: X.XXX)
```

**Что проверить:**
- ✅ Чанки действительно релевантны запросу
- ✅ Чанки содержат информацию, которая может ответить на вопрос
- ✅ Чанки не содержат только форматирование или метаданные

#### 6.2. Проверить, что чанки передаются в LLM
В логах TelegramBotService:
```
INFO  - Длина сформированного контекста: X символов
INFO  - Длина финального промпта: X символов
```

**Что проверить:**
- ✅ Контекст не пустой
- ✅ Контекст содержит найденные чанки
- ✅ Промпт правильно сформирован

---

### 7. 🚀 Возможные улучшения

#### 7.1. Увеличить topK
**Текущее значение:** 5 чанков

**Если чанки не подходят:**
- Увеличить до 10-15 чанков
- LLM сможет выбрать релевантную информацию из большего набора

**Как изменить:**
В `TelegramBotService.kt`:
```kotlin
val searchResult = ragSearchService.searchRelevantChunks(question, topK = 10)
```

#### 7.2. Добавить фильтрацию по порогу сходства
**Идея:** Не возвращать чанки с очень низким сходством (< 0.2)

**Реализация:**
В `RAGSearchService.kt`:
```kotlin
val topChunks = similarities
    .sortedByDescending { it.second }
    .filter { it.second >= 0.2 } // Минимальный порог
    .take(topK)
```

#### 7.3. Использовать гибридный поиск
**Идея:** Комбинировать семантический поиск (эмбеддинги) с ключевыми словами

**Реализация:**
1. Семантический поиск (текущий)
2. Поиск по ключевым словам (BM25 или TF-IDF)
3. Объединение результатов

#### 7.4. Переиндексировать с другой моделью
**Если текущая модель не работает:**
- Попробовать другую модель эмбеддингов
- Например: `all-minilm-l6-v2`, `multilingual-e5-base`

**Как изменить:**
```bash
export OLLAMA_EMBEDDING_MODEL=all-minilm-l6-v2
# Переиндексировать
/index
```

#### 7.5. Улучшить разбивку на чанки
**Текущая логика:** Разбивка по символам с попыткой разбить по границам предложений

**Улучшения:**
- Разбивать по параграфам (двойной перенос строки)
- Разбивать по заголовкам (markdown заголовки)
- Сохранять контекст заголовков в метаданных

---

### 8. 🐛 Типичные проблемы и решения

#### Проблема: Все значения сходства < 0.2
**Причины:**
1. Разные модели эмбеддингов для индексации и поиска
2. Плохая модель эмбеддингов
3. Запрос на другом языке, чем документы

**Решение:**
1. Проверить, что используется одна модель
2. Переиндексировать с правильной моделью
3. Использовать мультиязычную модель

#### Проблема: Чанки найдены, но не релевантны
**Причины:**
1. Слишком общий запрос
2. Чанки слишком большие (много шума)
3. Плохая разбивка на чанки

**Решение:**
1. Уточнить запрос
2. Уменьшить размер чанков
3. Улучшить логику разбивки

#### Проблема: Нужные чанки не попадают в топ-5
**Причины:**
1. topK слишком маленький
2. Низкое сходство из-за разных формулировок

**Решение:**
1. Увеличить topK до 10-15
2. Использовать переформулировку запроса (query expansion)

---

### 9. 📊 Метрики для оценки качества

#### 9.1. Precision (точность)
**Определение:** Сколько из найденных чанков действительно релевантны?

**Как измерить:**
- Вручную проверить топ-5 чанков
- Посчитать, сколько из них релевантны

**Цель:** > 60% (3 из 5 чанков релевантны)

#### 9.2. Recall (полнота)
**Определение:** Сколько релевантных чанков было найдено из всех существующих?

**Как измерить:**
- Зная, какие чанки должны быть найдены
- Проверить, попали ли они в топ-K

**Цель:** > 50% (хотя бы половина релевантных чанков найдена)

#### 9.3. Mean Reciprocal Rank (MRR)
**Определение:** На какой позиции находится первый релевантный чанк?

**Формула:** 1 / позиция_первого_релевантного_чанка

**Цель:** > 0.5 (первый релевантный чанк в топ-2)

---

### 10. 🔬 Отладка: Пошаговая диагностика

#### Шаг 1: Проверить базовые вещи
```bash
# 1. Индекс существует?
ls -lh index/index.json

# 2. Индекс не пустой?
cat index/index.json | jq '.totalChunks'

# 3. Ollama работает?
curl http://127.0.0.1:11434/api/tags
```

#### Шаг 2: Проверить логи при запросе
Выполнить `/rag <запрос>` и проверить логи:
1. Загружается ли индекс?
2. Генерируется ли эмбеддинг запроса?
3. Какие значения сходства?
4. Какие чанки в топ-5?

#### Шаг 3: Проверить качество чанков
Вручную открыть `index/index.json` и проверить:
1. Содержимое чанков
2. Размерность эмбеддингов
3. Значения эмбеддингов (не все нули)

#### Шаг 4: Тестовый запрос
Сделать простой запрос, который точно должен найти чанк:
```
/rag четвертый квартал
```

Если даже это не работает - проблема в базовой настройке.

#### Шаг 5: Сравнить эмбеддинги
Вручную сгенерировать эмбеддинг для запроса и для одного чанка, сравнить их сходство.

---

## ✅ Быстрая диагностика (чек-лист)

- [ ] Индекс создан и не пустой (`/indexinfo`)
- [ ] Ollama работает и модель загружена
- [ ] Используется одна модель для индексации и поиска
- [ ] Эмбеддинг запроса генерируется успешно (смотреть логи)
- [ ] Значения сходства > 0.2 (смотреть логи)
- [ ] Найденные чанки содержат релевантную информацию (проверить вручную)
- [ ] Запрос сформулирован конкретно и на том же языке, что документы
- [ ] Размер чанков подходит для типа вопросов (500-2000 символов)

---

## 🎯 Рекомендации по улучшению

1. **Всегда логировать значения сходства** - это ключевая метрика
2. **Добавить команду для тестирования поиска** - `/ragtest <запрос>` с выводом топ-10 чанков и их сходства
3. **Добавить метрики качества** - автоматически оценивать precision/recall
4. **Поддержка переформулировки запроса** - генерировать несколько вариантов запроса и искать по всем
5. **Кэширование эмбеддингов запросов** - не генерировать заново для похожих запросов

